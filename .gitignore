# regenerable outputs
assets/blender/renders/
assets/blender/*.blend
assets/blender/preview/
__pycache__/
*.pyc

# machine-local tool settings
.claude/settings.local.json

# installed packages (package-lock.json is committed; node_modules is not)
node_modules/

# visual review captures — regenerable by npm run review
docs/visual-review/

# repository-root scratch and recovery workspaces
/.tmp-*/
/.tmp_*

# regenerable test outputs written at the repository root
/map-layout-results.json
/playback-results.json
/*.log
