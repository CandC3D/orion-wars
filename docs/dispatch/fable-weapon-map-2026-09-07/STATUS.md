# STATUS

- 11:44 ACCEPTED; hard stop 12:14.
- 11:56 COMPLETE. Decision: not accepted as-is (F1 cold-bank outline, F2 centre-line boundaries, F3 compact key). Arithmetic verified 29/167/6 headings. Inventory 290/290. Queued to Astra; back to parked.
- 21:5x ORACLE REWRITTEN (Chris's request; supersedes the version this dispatch shipped). The original assumed the map drew one convex SECTOR per face: it read a sector's centreline vertex for rotation and tested coverage by point-in-polygon against that sector. The renderer has since moved to hex-grid outlines - a continuous sector edge through cell centres cannot express the engine's seam ruling - so BOTH checks had silently stopped meaning anything, reporting 28 rotation findings, 12 capped coverage findings and "non-seam mismatches 71970 of 1034712" against a map that was exactly correct. Anyone running it would have concluded the weapon map was badly broken.
  - The rewrite DECODES WHAT THE RENDERER DRAWS: every hex is one closed six-corner subpath, so averaging its corners and inverting the projection recovers the cell - without consulting `weaponCoverageCells`, which would have been the module marking its own homework. Expected ownership still comes from the written face convention, never the renderer's table.
  - `rotation` is now the real invariant: every cell drawn under a face must fall in the 60-degree wedge that face occupies at that heading, by screen angle, with seam cells exempt.
  - New checks for what shipped after the original: every arc in use must resolve to a NAMED preset (six had none until today); `effectiveBand` must report the outermost band still carrying the top modifier, and must not claim a graded weapon where there is no falloff; every shaded tier of the graded battery envelope must carry the best modifier any mount of that family offers at that cell.
  - The five movement findings it first produced were the oracle's own staleness, not a defect: since the cannon came under human control a COLD bank under automatic control deliberately reports no radius rather than promise one the engine may spend on charging. All three arms are now asserted - automatic refuses with a reason, manual hold-cold gives the plain ceiling, manual charge plants the hull.
  - RESULT against live: 29 hulls / 167 mounts / 6 headings; 1,034,712 cells checked; 284,364 drawn cells decoded; 268,770 rotation checks; 56,030 graded-tier cells; **0 seam mismatches of 49,500 seam cells** (the old version reported 619 it could not resolve), 0 non-seam mismatches, **0 findings**.
  - NEGATIVE CONTROLS, so an all-green run is not taken on trust: misassigning face 2 to face 1 produced 33,588 findings across `faces`, `rotation` and `coverage`; dropping every range-3 hex produced 12 `coverage` findings naming the exact hexes and moved the tier count 56,030 -> 54,842. The source file was restored byte-identical to HEAD after each.
  - The approvals list now includes `docs/drydock/vraygon-battleship-amendment-2026-09-07`.
